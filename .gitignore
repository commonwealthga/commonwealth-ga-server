out/
obj/
.cache/
compile_commands.json
Memory/
AGENTS.md


# Runtime / host-specific (never commit — control-server.json holds admin_token secret)
# .mcp.json wires up local MCP servers (the Ghidra bridge) by absolute path
.mcp.json
control-server.json
server.db
server.db-shm
server.db-wal

# Spectator overlay: icon assets + secrets stay local
tools/spectator-overlay/ICONS/
tools/spectator-overlay/GA_BROADCAST_ICONS.zip
tools/spectator-overlay/.env
__pycache__/

# The shareable console build, produced by `python gen3.py --public`. Regenerable from the
# same inputs as device-console.html, so it is not worth a second 3 MB blob per rebuild.
docs/claude/theorycraft-console/device-console-public.html
